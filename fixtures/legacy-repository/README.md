# invoicing

Run `node src/index.js`.

The `src/legacy/` directory is the original scheduled script. Most of it is
still live; `invoice-old.js` is not, but nobody has been brave enough to delete
it. Ask in #billing before touching anything in there.
