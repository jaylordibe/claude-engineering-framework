<!--
  Himoa — Claude Code entry point.

  Repository truth lives in AGENTS.md, the one neutral home every coding agent
  reads. This file exists only so Claude Code loads that same single source:
  the `@AGENTS.md` line below imports it. Do NOT copy repository facts here — a
  second copy of the truth drifts silently, which is the whole reason there is
  one home.

  Keep below the import only notes that are genuinely Claude Code-specific and
  true nowhere else. Most repositories need nothing here and delete the section.

  (Claude Code reads AGENTS.md natively; the `@AGENTS.md` import works whether or
  not that setting is on, and keeps this repository's truth in one file.)

  Delete every instruction comment before committing.
-->

# CLAUDE.md

@AGENTS.md

<!--
## Claude Code-specific notes

Only things true of Claude Code and nowhere else — e.g. a Claude-only skill this
repository ships in .claude/skills/. Repository truth (project, commands,
high-risk paths, consumers) belongs in AGENTS.md, not here.
-->
