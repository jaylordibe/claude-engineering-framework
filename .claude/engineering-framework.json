{
  "$schema": "../plugins/engineering-framework/reference/repo-config.schema.json",
  "frameworkVersion": "1.1.0",
  "commands": {
    "lint": "shellcheck plugins/engineering-framework/scripts/*.sh plugins/engineering-framework/bin/*",
    "test": "node tests/validate-plugin.mjs --strict && node tests/validate-fixtures.mjs && node tests/validate-charter.mjs && node tests/run-doctor-fixtures.mjs"
  },
  "risk": {
    "highRiskPaths": [
      "*/plugins/engineering-framework/scripts/session-charter.sh",
      "*/plugins/engineering-framework/bin/ef-doctor",
      "*/plugins/engineering-framework/hooks/hooks.json",
      "*/plugins/engineering-framework/.claude-plugin/plugin.json",
      "*/.claude-plugin/marketplace.json"
    ],
    "notes": "This repository ships methodology that runs in other people's repositories, and from 1.0.0 it ships no permission rules and no hooks that gate a command — nothing here can block a consumer's work, and no document may describe it as though it can. What can still go wrong is silent from here: a wrong standard makes an agent describe an architecture that does not exist, and a wrong charter line is paid on every request in every installed repository. Consumers may auto-update, so the version bump in plugin.json is the only brake between a changed standard and all of them."
  }
}
